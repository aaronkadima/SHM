FROM python:3.11-slim
WORKDIR /app
COPY requirements-cdm.txt .
RUN pip install --no-cache-dir -r requirements-cdm.txt
COPY app ./app
ENV SHM_ENGINE_ID=cdm_1
EXPOSE 8000
CMD ["sh","-c","uvicorn app.standalone:app --host 0.0.0.0 --port ${PORT:-8000}"]
